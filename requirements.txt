# CineMatch — AI Movie Finder
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6
google-generativeai>=0.8.0
httpx>=0.28.0

# Optional: for voice transcription (uses Perplexity/ElevenLabs Scribe via pplx SDK)
# If you have access to pplx.python.sdks.llm_api, install that package to enable /api/transcribe
